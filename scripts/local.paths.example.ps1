# Copy to scripts\local.paths.ps1 (gitignored) only if automatic discovery fails.
# Both build.ps1 and install.ps1 pick these up.

# $script:LocalDspDir     = 'C:\Program Files (x86)\Steam\steamapps\common\Dyson Sphere Program'
# $script:LocalBepInExDir = "$env:APPDATA\com.kesomannen.gale\dyson-sphere-program\profiles\<profile>\BepInEx"
